import { Widget } from "@typeform/embed-react";

const Typeform = () => {
  
  // if you do use Typeform, you just need to grab your form's ID & drop it in here
  return (
    <Widget id="k9cQ3bTD" style={{ width: '80%', height:'500px'}} />
  )
}

export default Typeform;